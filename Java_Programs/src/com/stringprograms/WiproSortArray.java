package com.stringprograms;

import java.lang.reflect.Array;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;
import java.util.LinkedHashSet;
import java.util.List;
import java.util.Set;

public class WiproSortArray {
	
	public static void main(String[] args) {
		
	/*int[] arr={2, 1, 2, 10, 5, ' ', 19, 5, 5, ' ', 120};

	//create an arraylist from the array
    // using asList() method of the Arrays class
   // ArrayList<Integer> numbers = new ArrayList<>(arr.length);
    List lst = Arrays.asList(arr);
    System.out.println("ArrayList with duplicate elements: " + lst);

    // convert the arraylist into a set
    Set<Integer> set = new LinkedHashSet<>();
    set.addAll(numbers);

    // delete al elements of arraylist
    numbers.clear();

    // add element from set to arraylist
    numbers.addAll(set);
  //  System.out.println("ArrayList without duplicate elements: " + numbers); */
  }
}
